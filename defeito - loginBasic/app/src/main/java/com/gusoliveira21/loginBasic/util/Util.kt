package com.gusoliveira21.loginBasic.util

import android.content.Context
import android.widget.Toast

//TODO: Área destinada a redução de código

class Util() {
    //private lateinit var binding:DataBindingUtil//Cadastro: ActivityCadastroBinding// by lazy{ ActivityCadastroBinding.inflate()}

    fun opcoesErro(errorValue: String, context: Context, findViewById: Any) {
/*
            if(errorValue.contains("password is invalid")) binding.etEmailLogin.setError("Digite uma senha mais forte! \n A cima de 6 dígitos!")
            else if(errorValue.contains("is already in use")) binding.etEmailLogin.setError("O endereço de email já está sendo usado em outra conta!")
            else if(errorValue.contains("badly formatted")) binding.etEmailLogin.setError("Forneça corretamente o email!")
            else if(errorValue.contains("user may have been deleted")) binding.etEmailLogin.setError("Email não cadastrado!")
            else if(errorValue.contains("password is invalid")) binding.etEmailLogin.setError("Senha incorreta!")
            else Toast.makeText(context, "Houve um erro ao cadastrar usuário!", Toast.LENGTH_SHORT).show()
*/
    }


    /*
    fun setMessageOfErrorPassword(mensagem: String) {
            bindingCadastro.etSenhaUmCadastro.setError(mensagem)
            bindingCadastro.etSenhaDoisCadastro.setError(mensagem)
        }
    */

}